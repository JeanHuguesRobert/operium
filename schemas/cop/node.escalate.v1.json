{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "cop/node.escalate.v1",
  "title": "COP node escalate Event — deadline or policy breached, still not a mandate",
  "type": "object",
  "required": ["id", "packet_type", "payload"],
  "properties": {
    "id": { "type": "string", "minLength": 1 },
    "packet_type": { "const": "cop/node.escalate.v1" },
    "created_at": { "type": "string", "format": "date-time" },
    "payload": {
      "type": "object",
      "required": ["schema", "obligation_id"],
      "properties": {
        "schema": { "const": "cop/node.escalate.v1" },
        "obligation_id": { "type": "string", "minLength": 1 },
        "reason": { "type": ["string", "null"] }
      },
      "additionalProperties": true
    }
  },
  "additionalProperties": true
}
